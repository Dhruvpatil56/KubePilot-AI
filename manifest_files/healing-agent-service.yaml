apiVersion: v1
kind: Service
metadata:
  name: healing-agent
  namespace: sre-demo
  labels:
    app: healing-agent
spec:
  selector:
    app: healing-agent
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
